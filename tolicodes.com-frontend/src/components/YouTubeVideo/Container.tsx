import styled from "styled-components";
const decoration = require("./assets/decoration.png");

const Container = styled.div`
  height: 600px;
  margin-bottom: 180px;
  // background: url(${decoration}) no-repeat bottom left;
`;
export default Container;
