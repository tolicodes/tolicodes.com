import styled from "styled-components";

export const SSiteSubtitle = styled.p`
  font-family: PT Mono;
  font-style: normal;
  font-weight: normal;
  font-size: 20px;
  line-height: 22px;
  text-align: center;
  letter-spacing: 0.498221px;

  color: var(--ui-03);
`;
