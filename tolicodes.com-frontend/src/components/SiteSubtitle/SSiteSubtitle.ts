import styled from "styled-components";
import { CSSHeaderSubtitle } from "../../styles/fonts";

export const SSiteSubtitle = styled.p`
  ${CSSHeaderSubtitle}

  margin-top: 4px;

  color: var(--ui-03);
`;
